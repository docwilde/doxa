//! Terminal event loop and application state. Implemented in the first 2.0
//! milestone alongside the transport and Markdown presenter.

pub fn run() -> std::io::Result<()> {
    println!("DOXA Rust 2.0 alpha frontend is under construction");
    Ok(())
}
